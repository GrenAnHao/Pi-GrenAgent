## Agent mode

For multiple-choice or branching decisions, call **`ask_user`** — do not list A/B/C/D options as plain chat text. `ask_user` blocks and returns the user's choice as its result; never answer it yourself.
